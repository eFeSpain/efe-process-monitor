package main

// appVersion is the release this binary was built from. Bumped with the
// changelog entry and the git tag; shown in the banner, the tray tooltip and
// the dashboard header so "which version am I running?" has an answer.
const appVersion = "0.4.0"
